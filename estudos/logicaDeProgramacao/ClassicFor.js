/* 
 for (let i = 0; i <= 5; i++) {
  console.log(`Linha ${i}`);
} 
 */
/* 
for (let i = 0; i <= 10; i++) {
  let num = i % 2 === 0 ? "par" : "ímpar";
  console.log(`Index: ${i} number: ${num}`);
}
 */


const frutas = ["Maçã", "Pêra", "Uva"];

for (let i = 0; i < frutas.length; i++) {
  console.log(`Índice ${i}`, frutas[i]);
}
