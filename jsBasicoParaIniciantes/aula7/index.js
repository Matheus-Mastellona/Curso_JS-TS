const nome = "João"; // o valor da constante não pode ser alterado
// constante deve ser atribuida o valor ao iniciar
console.log(nome);

// da erro => const nome;
// da errp => consto nome = "x"; - nome = 'y'

console.log(typeof(nome)); // verificando a tipagem da variável

const numero = '4';
const segundoNumero = 50;

console.log(numero+segundoNumero);// concatenação, pois são variáveis de tipagens diferentes